fix: resolve TypeScript build error with Netlify Identity

- Fix TypeScript error with `@ts-expect-error` directive
- Use dynamic import with { ssr: false } to properly load client components
- Add ClientOnly wrapper component for cleaner integration
- Ensure proper client/server component boundaries

This fixes the build error: "Unused '@ts-expect-error' directive"
